<% Category.all.each do |c| %>
  <h5><%= c.name %></h5>
  <div class="well">
    <% c.lessons.each do |l| %>
      <div class="row-fluid" style="padding: 5px;">
        <a href="/lessons/<%= l.id %>"><%= l.name %></a>
      </div>
    <% end %>
  </div>
<% end %>